#include <math.h>


void s_Start();			// Funky use of capitals to be compliant with other PSOC libs
char s_do_measure(unsigned char index);
int  s_getCurrentTemp(unsigned char index);
unsigned char  s_getCurrentHumi(unsigned char index);
